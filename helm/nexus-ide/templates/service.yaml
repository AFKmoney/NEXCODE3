apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-ide.fullname" . }}
  labels:
    {{- include "nexus-ide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-ide.selectorLabels" . | nindent 4 }}
